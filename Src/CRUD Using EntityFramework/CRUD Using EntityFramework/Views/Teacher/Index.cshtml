@model IEnumerable<CRUD_Using_EntityFramework.Models.tbl_Teacher>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Add New Teacher", "Add","",new {@class="btn btn-primary"})
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Teacher_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Contact)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Qualifcation)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Teacher_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Contact)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Qualifcation)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TeacherID }, new { @class = "btn btn-sm btn-primary" })&nbsp;
            @Html.ActionLink("Delete", "Delete", new { id=item.TeacherID }, new { @class = "btn btn-sm btn-danger" })
        </td>
    </tr>
}

</table>
